# 1.本章主要内容
1.  在前七章的底层数据结构的前提下，介绍API层面五种数据类型的底层实现；已经实现过程中的各种小优化
2.  简单介绍了redis使用的自动回收机制——引用计数

# 2.读书笔记
1.  每种类型都会有不同的数据结构来实现，具体是那个数据结构，针对数据量，是有明确规定的，而且规定可以配置
2.  会有"整数字符串"常量池，而不会有字符串常量池，因为后者在判断equal时时间复杂度不可能达到O(1)
3.  redis使用引用计数的方式来标记'roj'对象是否能回收。
    redis用lru值来表示一个roj多久没有被访问，从而在开启memory机制时，淘汰那些好久没被访问的。
    注意1：回收和淘汰不是一个东西，虽然他们都会造成内存回收
    注意2：memory机制开启，redis就彻底是一个缓存了，即cache模式而不是store模式。
    问题：memory机制还是不清楚呢？
4.  
